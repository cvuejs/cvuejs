import { HttpSend } from '@cvue/http'

class Api {
  list(params?: any) {
    return HttpSend({
      url: '/admin/list',
      params,
      method: 'GET'
    })
  }
}

export const The<%= classify(name) %>Api = new Api();
